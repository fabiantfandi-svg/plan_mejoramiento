// Verifico si estoy logueado usando el ternario
let Torres = true;

// El ternario me deja devolver un mensaje sin usar if
let Fandiño = Torres ? "Usuario autenticado" : "Acceso denegado";

console.log(Fandiño);
